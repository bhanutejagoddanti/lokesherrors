apiVersion: v1
kind: Service

metadata:
  name: workday-backend-service

spec:
  selector:
    app: workday-backend

  ports:
    - protocol: TCP
      port: 4001
      targetPort: 4001

  type: ClusterIP